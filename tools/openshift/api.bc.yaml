---
apiVersion: v1
kind: Template
labels:
  template: 'educ-grad-report-api-template'
metadata:
  name: 'educ-grad-report-api-template'
objects:
- apiVersion: v1
  kind: BuildConfig
  metadata:
    labels:
      build: educ-grad-report-api-pipeline
    name: educ-grad-report-api-pipeline
    namespace: 77c02f-tools
  spec:
    source:
      git:
        ref: main
        uri: 'https://github.com/bcgov/EDUC-GRAD-REPORT-API.git'
      type: Git
    strategy:
      dockerStrategy:
        pullSecret:
          name: artifactory-creds
        env:
          - name: BUILD_LOGLEVEL
            value: '2'
        type: Docker
